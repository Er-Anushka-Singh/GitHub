auto auto config --> to come pop email and user Name
git CUL --> Command line Interface

GUI --> Graphical user interface --> (by clicking initialized respositories)

#To set globally git 
1) git config --global user.name "Anushka Singh"
2) git config --global user.email "anushkalucky11@gmail.com"
3) git config --global --list
4) git config --global core.editor "code --wait";
5) git config --global core.autocrlf true




# Git Useful Command (CUL )
1) git init
U= untracked
A= Added
M= Modified
R=Remove
D=Delete
!= conflict

2) git add [f1.md]

3) git rm --cached [f1.md] (It will  delete the memory from .git. It work go one step back)

4) git restore --staged [f1.md] (It will not delete the memory . It work go one step back)

# commit

1) git commit -m "First commit" (commit message)

commit object (who->(author,commit),
when->(Date and time),
parent->(none hash),
commitId->(40 ALPHA NUMERIC CHARACTER),
commit message->(first commit))

2) git commit --amend -m "New commit Message"

3) git commit --amend --no-edit

# Rename a Branch

1) a. git branch -m [master] = respectfull
   b. git branch -M [master] = forcefull

2) git branch -m main(Name written should be small)

# .gitignore
1)  .gitignore

# To coonect Local respository {GENAI folder} to remote respository (GENAI github folder )
1) git remote add origin https://github.com/Er-Anushka-Singh/GEN-AI.git

# upload our code to github
1) git push -u origin main

# Day-3

# To create branch 
1) git branch [branchName] --> (It create the branch only)
2) git checkout -b [branchName] -->(create and switch the branch)
3) git switch -c [branchName]  -->(Forcefully)


# To delete branch

1) git branch -d [branchName] => safe
2) git branch -D [branchName] => ForceFully


# Delete a remote branch
1) git push origin --delete [branchName]


# Merge
1) git merge [branchName]


# To see all branch 
1) git branch -a

# branches
1) git branch [branchname]
2) git checkout -b [branchname] or 
3) git switch -c[branchname]

# Rename:
1) git branch-m[newbranchname]
2) git branch -M[newbranchname]

# deletebrnach:
1) git branch -d[branchname]
2) git branch -D[branchname]

# Delete a remote branch 
1) git push origin --delete[branchname]

# Merge 
1) git merge [branchname]

# To see all branches
1) git branch -a

# stash 

1) git stash
2) git stash apply
3) git stash drop "stash@{0}"
4) git stash clear
  window+v to show GIF


# technique for merge
   fast forward merge
   3 way merge
   first time git push -u origin main (main ka sara change origin mein dal do )
   but for second time 
   git push origin main 


# To make clone inside local respositry (open cmd on that location) done by team lead

1) code .
   git clone [addPath]

# To make clone by developers

1) git clone [addPath]

# Important point 
1)  Developer does not work in main branch 
      git branch [feature/loginform]

# If you forget making branch

 1)  git branch

# If we push by other branch 

1) git push -u origin [feature/loginform]
