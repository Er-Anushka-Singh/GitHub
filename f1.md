auto auto config --> to come pop email and user Name
git CUL --> Command line Interface

GUI --> Graphical user interface --> (by clicking initialized respositories)

#To set globally git 
1) git config --global user.name "Anushka Singh"
2) git config --global user.email "anushkalucky11@gmail.com"
3) git config --global --list
4) git config --global core.editor "code --wait";
5) git config --global core.autocrlf true




# Git Useful Command (CUL )
1) git init
U= untracked
A= Added
M= Modified
R=Remove
D=Delete
!= conflict

2) git add f1.md

3) git rm --cached f1.md (It will  delete the memory from .git. It work go one step back)

4) git restore --staged f1.md (It will not delete the memory . It work go one step back)

# commit

1) git commit -m "First commit" (commit message)

commit object (who->(author,commit),
when->(Date and time),
parent->(none hash),
commitId->(40 ALPHA NUMERIC CHARACTER),
commit message->(first commit))

2) git commit --amend -m "New commit Message"

3) git commit --amend --no-edit

# Rename a Branch

1) a. git branch -m master = respectfull
   b.   git branch -M master = forcefull

2) git branch -m main(Name written should be small)

# .gitignore

# To coonect Local respository {GENAI folder} to remote respository (GENAI github folder )

1) git remote add origin https://github.com/Er-Anushka-Singh/GEN-AI.git

# upload our code to github

1) git push -u origin main


